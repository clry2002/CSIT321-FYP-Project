// const config = {
//   plugins: ["@tailwindcss/postcss"],
// };

// export default config;

// Modify for deployment purposes
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;