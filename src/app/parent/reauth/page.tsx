// app/parent/reauth/page.tsx
'use client';

import ParentReauth from '../../components/parent/reauth';

export default function ParentReauthPage() {
  return <ParentReauth />;
}

// Metadata is declared in a separate file (layout.tsx) since this is a client component